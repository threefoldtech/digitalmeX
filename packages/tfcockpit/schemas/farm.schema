@url = tfgrid.farm.1
3bot_id* = (S) 
iyo_organization* = (S) # for backward compatibility
name* = (S)
wallet_addresses = (LS)
location = (O) !tfgrid.location.1
email = (email)
vta = (S)
resource_price = (O) !tfgrid.node.resource.1